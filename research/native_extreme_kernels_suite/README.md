# `native_extreme_kernels_suite`

<!-- generated by research/build_suite_readmes.py -->

Evidence suite: 5 container(s), 5 reference output(s), 5 input file(s); no machine-readable `board_results_*.json` in this suite; its recorded run lives in the board ledger and/or the notes below.

The manifest for this suite is not a list of model entries; read it directly.

## Files

* containers: `modelNNN.bin` (5)
* reference outputs: `expectedNNN.i8` (5)
* inputs: `inputNNN.u8` (5)
* `manifest.json`, `board_results_*.json`, `board_summary.txt`

## Reproduce

* regenerate the suite: `PYTHONPATH=src python research/build_native_extreme_kernels.py`
* run it on the board: `make board-suite SUITE=native_extreme_kernels_suite` (needs `$ADB` and the cross toolchain)
* recompile without hardware: `PYTHONPATH=src python research/verify_suites.py`

## What this suite pins

* **Profile(s)**: the manifest names no profile over 0 container(s).
* **Container format(s)**: unknown.
* **Cases**: 5 input file(s), 5 reference output file(s), 6496 reference byte(s).
* **Generator**: `research/build_native_extreme_kernels.py`; recompiling it is the reproducibility check (`research/verify_suites.py`).

This page is generated from the manifest and the containers themselves by
`research/build_suite_readmes.py`; `--check` fails when it drifts. For hand-written
context (what the bound means, what failed first, which register field it exercises),
edit the page and delete the `<!-- generated by research/build_suite_readmes.py -->` marker: the generator then leaves it alone.
