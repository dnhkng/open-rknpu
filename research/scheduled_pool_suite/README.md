# `scheduled_pool_suite`

<!-- generated by research/build_suite_readmes.py -->

Evidence suite: 12 container(s), 12 reference output(s), 12 input file(s); no machine-readable `board_results_*.json` in this suite; its recorded run lives in the board ledger and/or the notes below.

The manifest for this suite is not a list of model entries; read it directly.

## Files

* containers: `modelNNN.bin` (12)
* reference outputs: `expectedNNN.i8` (12)
* inputs: `inputNNN.u8` (12)
* `manifest.json`, `board_results_*.json`, `board_summary.txt`

## Reproduce

* run it on the board: `make board-suite SUITE=scheduled_pool_suite` (needs `$ADB` and the cross toolchain)
* recompile without hardware: `PYTHONPATH=src python research/verify_suites.py`

## Notes

Add what this suite demonstrates, the bound it pins and any caveat here; this section is
never regenerated once you edit the file (the marker only matters for untouched pages).
